# Environments
.env
.venv/
venv/
ENV/
env/

# Python Cache
__pycache__/
*.py[cod]
*$py.class
*.so

# Jupyter Notebooks
.ipynb_checkpoints

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo

# Testing and Coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
nosetests.xml
coverage.xml

# Databases (Upcoming in Phase 5)
*.db
*.sqlite
*.sqlite3

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# RAG source corpora — large external git clones (MicrosoftDocs/
# dataexplorer-docs, MicrosoftDocs/defender-docs), not project source.
# Re-fetch via the clone commands in PROJECT_STATUS.md §4AB / re-derive
# via src/retrieval/build_indexes.py. The built indexes themselves
# (data/rag_indexes/*.pkl) ARE small and ARE committed — they're the
# actual build artifact this project depends on at runtime.
.rag_corpora/

# Blog drafts — publishing artifacts, not project source
blogs/
