[pytest]
markers =
    regression_gate: permanent should-pass anchors, one per historically-fixed bug class (PROJECT_STATUS.md §4Z) — run via `pytest -m regression_gate` after ANY prompt/agent edit, before considering the edit complete.
