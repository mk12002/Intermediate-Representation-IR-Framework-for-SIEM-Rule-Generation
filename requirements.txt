# Core
pydantic>=2.7.0
python-dotenv>=1.0.1

# LLM & Agents — provider fixed once for the whole study via LLM_PROVIDER
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.2.0
langchain-ollama>=0.1.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0

# KQL generation
jinja2>=3.1.4

# Dataset processing
pandas>=2.2.0
numpy>=1.26.0
pyyaml>=6.0.1

# Evaluation / statistics
scipy>=1.13.0
statsmodels>=0.14.0
codebleu>=0.6.1

# Testing
pytest>=8.2.0

# Frontend demo (app.py)
streamlit>=1.35.0

# RAG retrieval — TF-IDF + cosine similarity, no embedding API needed
# (see src/retrieval/retriever.py and src/retrieval/build_indexes.py).
scikit-learn>=1.3.0
